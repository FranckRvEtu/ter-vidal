import mongoose from "mongoose";
import RDV from "../models/RDV.js";


/*const rdvTestData = {
    date: new Date(),
    idPatient: new mongoose.Types.ObjectId(),
    lieu: 'Test'
};

const addRDVToDB = async () => {
    const newRDV = new RDV(rdvTestData);
    try {
        const rdv = await newRDV.save(); // Attendre la sauvegarde du RDV
        console.log('RDV ajouté avec succès');
    } catch (error) {
        console.error('Erreur lors de l\'ajout du RDV:', error);
    }
};

export default addRDVToDB;*/